# Copy this file to .env and fill in your values
# cp .env.example .env

# Required
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional - defaults shown
ANTHROPIC_MODEL=claude-sonnet-4-20250514
ANTHROPIC_MAX_TOKENS=500

# Agent behavior (for future use)
AGENT_CONFIDENCE_THRESHOLD=0.8
AGENT_TIMEOUT_SECONDS=30

# BOH Integration - Direct DB connection for sub-20ms lead delivery
# Same PostgreSQL instance as Kitchen. If not set, leads captured but not sent to BOH.
# Format: postgresql+asyncpg://user:password@host:port/database
BOH_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/boh

# Chainlit Data Persistence - Saves conversation history
# Optional - if not set, conversations are not persisted across sessions
# Can be same DB as BOH or separate. Tables auto-created: users, threads, steps, elements, feedbacks
# Format: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/chainlit
